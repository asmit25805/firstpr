# FirstPR client

The React + Vite frontend. See the [root README](../README.md) for setup instructions — this
workspace isn't meant to be run standalone (it expects the server running alongside it).
